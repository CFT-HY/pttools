[tool.pytest.ini_options]
addopts = "--alluredir=test-results/allure --cov=. --cov-report=html --cov-report=term --cov-report=term-missing --cov-report=xml --html=test-results/html/test-report.html --numprocesses=auto"
# python_paths = "tests"
testpaths = [
    "tests"
]
